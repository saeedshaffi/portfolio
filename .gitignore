# macOS junk
.DS_Store
**/.DS_Store

# Local scratch folders (never commit)
_backups/
_figma_tmp/
_stale_locks/
_to_delete/
trash/

# Editor/tooling
.vscode/
*.log
dist/

# Local push helper + its log (not part of the site)
push-log.txt
push-to-github.command

# Showreel source frames and draft cuts (60 MB, not part of the site)
showreel-src/
